# Reports
reports: --format pretty --format AllureCucumber::CucumberFormatter --out allure-results

# Brands e Environments
hlg: HOST='https://www.amazon.com.br/'


# Execution device
desktop: DEVICE=desktop
mobile: DEVICE=mobile

# Graphic mode
headless: GRAPHIC_MODE=headless
graphic:  GRAPHIC_MODE=graphic

# Execution
remote: MODE=remote
local:  MODE=local